import './Header.css';

const Header = () => {
  return (
    <div className="header__container">
      <h1 className="header__txt">Check if a graph is red-blue colorable</h1>
    </div>
  );
};

export default Header;
